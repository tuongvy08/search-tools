# Phase 6C0.1 — Team capabilities (local)

- **Phạm vi:** worktree, branch `codex/phase6c0-1-team-capabilities`. Không deploy, SSH/VPS, staging/production hay merge main.
- **Audit:** tận dụng session validation, IP/brand visibility, CSRF, actor revalidation và preview/confirm PostgreSQL hiện hữu. Registry mới gồm 8 chức năng và 9 cột; cấu hình theo team, admin bypass, LOCAL/GOOGLE dùng cùng policy đọc mới mỗi request.
- **Mặc định:** migration 021 additive/atomic/idempotent, team cũ giữ đủ 17 grants. Không dùng SQL enum cho danh sách quyền. Quyền mới trong registry cần grant tường minh. Preview cũ thiếu capability snapshot giữ nguyên grants hiện tại.
- **Quy tắc:** SEARCH_BY_CAS độc lập VIEW_CAS; Check License cần SEARCH_BY_CAS + VIEW_COMPLIANCE; Advanced Search cần SEARCH_BY_CAS; Quick Quote cần VIEW_PRICE. Copy/Export chỉ chứa cột được xem, không tự cấp quyền xem giá.
- **Boundary:** chặn route/action ở backend, loại aliases và dữ liệu lồng trước JSON; Copy kiểm tra lại quyền/CSRF, Quote Copy đọc lại sản phẩm/eligibility. Search Export trả TSV. Quote Export khi có cột ẩn tạo workbook mới với cột được phép, không mang nội dung/template/shared strings cũ; đầy đủ quyền giữ exporter/template hiện hữu. Capability history lưu before/after và actor cùng transaction với audit event.
- **Migration rehearsal:** psql chạy 021 hai lần trên DB tạm sau 015–020; dữ liệu team/user và 17 grants mặc định được giữ, rerun không ghi đè policy đã sửa hay sinh audit thừa. Script: `scripts/rehearsal_phase6c01.py`.
- **Security review:** 1 finding về mã lý do/counter tiết lộ Compliance đã sửa thành trạng thái điều kiện chung; test sản phẩm bị chặn và fallback xác nhận dữ liệu ẩn không ra API, vẫn chặn chọn/copy. Không còn finding chưa xử lý trong phạm vi review.
- **Browser QA:** admin và profile sinh học/không giá/Sales ở 1440px/390px; kiểm tra header, ô dữ liệu, copy thực tế, trạng thái phụ thuộc, preview diff và overflow. Search/Quick Quote không tràn ngang ngoài vùng bảng cuộn. Sửa Match còn gọi jQuery đã bỏ khỏi trang, dùng fetch. Nút confirm trong browser bị automatic approval review chặn vì chưa có xác nhận riêng cho thay đổi quyền QA; preview được đóng, không áp dụng. Confirm/CSRF/ownership/concurrency/stale preview được kiểm tra bằng integration tests trên DB tạm khác.
- **Validation:** full suite chạy đúng 1 lần: **893 tests, OK, 3 skipped** vì không có `QUOTE_TEMPLATE_FIXTURE` cho template thực bên ngoài repo. Các test workbook bằng fixture tổng hợp và restricted export đều đạt. Focused regression đạt; `git diff --check` sạch. Server và DB QA/rehearsal đã dọn. Không benchmark riêng.
